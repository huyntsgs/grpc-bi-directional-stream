syntax = "proto3";

package protobuf;

service Math {
	rpc FindMaxNumber(stream Request) returns (stream Response);
}

message Request {
	int32 number = 1;
	bytes sig = 2;	
	bytes publicKey = 3;
	bytes r = 4;
	bytes s = 5;
}

message Response {
	int32 res = 1;
}